apply plugin: "war"

bootWar {
    mainClass = "com.anthonylldev.streaming.StreamingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
